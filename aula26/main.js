// # Aula sobre operações ternarias 

// (condição) ? 'valor para verdadeiro' : 'valor para falso' 
// apos o " ? " vem a condição verdadeira
// apos o " : " vem a condição fallback caso a primeira condição não seja satisfeita

const pontucaoUsuario = 1000;
const nivelUsuario = pontucaoUsuario >= 1000 ? 'usuario VIP!' : 'usuario normie!';
console.log(nivelUsuario);

// ele vai substituir if else que são que tem apenas duas condições 
// além de poder ser utilizado dentro de alguma variável e assim podendo ser utilizado sobre todo o código 










